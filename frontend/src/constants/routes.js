const routes = {
  dashboard: '/',
  login: '/login',
  users: '/users',
  documents: '/documents',
  publications: '/publications',
  access: '/access',
  reports: '/reports',
  settings: '/settings',
};

export default routes;
